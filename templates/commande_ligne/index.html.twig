{% extends 'base.html.twig' %}

{% block title %}CommandeLigne index{% endblock %}

{% block body %}
    <h1>CommandeLigne index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commande_ligne in commande_lignes %}
            <tr>
                <td>{{ commande_ligne.id }}</td>
                <td>{{ commande_ligne.quantity }}</td>
                <td>
                    <a href="{{ path('app_commande_ligne_show', {'id': commande_ligne.id}) }}">show</a>
                    <a href="{{ path('app_commande_ligne_edit', {'id': commande_ligne.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_commande_ligne_new') }}">Create new</a>
{% endblock %}
